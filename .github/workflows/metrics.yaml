---
name: 📈 Forge metrics
"on":
  workflow_dispatch:
  schedule:
    - cron: "41 5 * * 2"

jobs:

  metrics:
    uses: kdeldycke/repomatic/.github/workflows/metrics.yaml@35cc8a45d65954faebaf8b4bb9960b525d446554 # v7.16.2
    secrets:
      REPOMATIC_PAT: ${{ secrets.REPOMATIC_PAT }}
