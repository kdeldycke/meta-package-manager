---
name: 📈 Forge metrics
"on":
  workflow_dispatch:
  schedule:
    - cron: "41 5 * * 2"

jobs:

  metrics:
    uses: kdeldycke/repomatic/.github/workflows/metrics.yaml@2e7b4cd29c6decc9304bc810be03e83844077644 # v7.16.0
    secrets:
      REPOMATIC_PAT: ${{ secrets.REPOMATIC_PAT }}
