---
name: 🚀 Build & release
"on":
  push:
    branches:
      - main
  workflow_dispatch:

jobs:

  release:
    uses: kdeldycke/repomatic/.github/workflows/release.yaml@ac35a90ce5322a4e4593ae0db7cf348cec80952f # v6.10.0
    secrets:
      PYPI_TOKEN: ${{ secrets.PYPI_TOKEN }}
      REPOMATIC_PAT: ${{ secrets.REPOMATIC_PAT }}
