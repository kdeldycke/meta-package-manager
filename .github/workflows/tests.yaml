---
name: 🔬 Tests
"on":
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - .github/workflows/tests.yaml
      - changelog.md
      - docs/**
      - gnome-shell/**
      - meta_package_manager/**
      - pyproject.toml
      - readme.md
      - tests/**
      - uv.lock
  pull_request:
    branches-ignore:
      # This filter matches the PR's *base* branch (always `main`), not its
      # head, so it does not actually exclude these PRs: see
      # https://github.com/actions/runner/issues/1591. Kept here as
      # documentation of intent, since the skip only becomes real through a
      # `github.head_ref` check on a job's `if:`, propagated through `needs:`.
      # Named no more precisely than that because `repomatic init
      # workflows/tests.yaml` copies this header into downstream repos, which
      # own their jobs and carry no `metadata` job for it to point at.
      - major-version-increment
      - minor-version-increment
      - prepare-release
    paths:
      - .github/workflows/tests.yaml
      - changelog.md
      - docs/**
      - gnome-shell/**
      - meta_package_manager/**
      - pyproject.toml
      - readme.md
      - tests/**
      - uv.lock
  schedule:
    # Run tests monthly to catch regressions from dependency or environment changes.
    - cron: "17 9 1 * *"

permissions: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ !startsWith(github.event.head_commit.message, '[changelog] Release') }}

# Supply-chain cooldown: no package published within the window can be resolved by
# any command in this workflow. Set here, not per command, so it also covers the
# `metadata` bootstrap and any step added later; a workflow-level `env:` cannot
# reference `needs`, so the window is a literal kept equal to `[tool.repomatic]
# minimum-release-age`. repomatic's own test suite enforces that upstream; a
# synced copy is kept in step by hand. Deliberate bypasses are per-package CLI
# flags (`--exclude-newer-package`, `--min-release-age-exclude`).
# See claude.md for the rationale.
env:
  NPM_CONFIG_MIN_RELEASE_AGE: 7
  UV_EXCLUDE_NEWER: "1 week"

jobs:

  metadata:
    name: 🧬 Project metadata
    # Skipping this job propagates to every `needs: metadata` job below.
    # The `pull_request.branches-ignore` filter above matches the PR's base
    # branch (always `main`), not its head branch (see
    # https://github.com/actions/runner/issues/1591), so the version-bump
    # PRs slip through it and must be filtered here via `github.head_ref`.
    # The branch list mirrors `repomatic.binary.VERSION_BUMP_BRANCHES` and
    # is pinned by repomatic's own test suite.
    #
    # The `[changelog] Post-release bump ` commit prefix is intentionally
    # *not* in this list: that push also carries the release commit, and
    # the post-release-bump head commit is our only post-merge test of the
    # release-frozen tree. The commit-prefix list mirrors
    # `MANUAL_VERSION_BUMP_COMMIT_PREFIXES` and is pinned by repomatic's own
    # test suite.
    if: >
      !contains(fromJSON('["major-version-increment","minor-version-increment","prepare-release"]'),
      github.head_ref)
      && !startsWith(github.event.head_commit.message || '', '[changelog] Bump major version to ')
      && !startsWith(github.event.head_commit.message || '', '[changelog] Bump minor version to ')
    runs-on: ubuntu-26.04
    timeout-minutes: 10
    outputs:
      metadata: ${{ steps.metadata.outputs.metadata }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          # Pin what CI downloads: unpinned, setup-uv installs the newest uv
          # satisfying `required-version`. `sync-workflow-pins` bumps it once a
          # uv release clears the cooldown window.
          version: "0.12.5"
      - name: Run repomatic metadata
        id: metadata
        run: >
          uvx --no-progress --exclude-newer-package repomatic=P0D 'repomatic==7.14.0' metadata
          --format github-json --output "$GITHUB_OUTPUT"
          test_matrix test_matrix_pr

  tests:
    needs: metadata
    strategy:
      # We delegate the failure decision to each job, by the way of the "continue-on-error" flag.
      fail-fast: false
      matrix: >-
        ${{ github.event_name != 'pull_request'
        && fromJSON(needs.metadata.outputs.metadata).test_matrix
        || fromJSON(needs.metadata.outputs.metadata).test_matrix_pr }}
    name: "${{ matrix.state == 'stable' && '✅' || '⁉️' }} ${{ matrix.os }} / py${{ matrix.python-version }}"
    runs-on: ${{ matrix.os }}
    timeout-minutes: 180
    # We keep going when a job flagged as not stable fails.
    continue-on-error: ${{ matrix.state == 'unstable' }}
    env:
      # XXX Workaround for Windows runners redirecting the output of commands to files. See:
      # https://github.com/databrickslabs/dbx/issues/455#issuecomment-1312770919
      # https://github.com/pallets/click/issues/2121#issuecomment-1312773882
      # https://gist.github.com/NodeJSmith/e7e37f2d3f162456869f015f842bcf15
      PYTHONIOENCODING: "utf8"
      # Deactivate Homebrew verbose output and auto-update.
      HOMEBREW_NO_ENV_HINTS: "1"
      HOMEBREW_NO_AUTO_UPDATE: "1"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-tags: true

      ### Install or upgrade package managers on each platform, and made them
      ### ready to use for mpm.

      # Pip
      - name: Pip & Setuptools - Upgrade
        run: python -m pip install --upgrade setuptools pip

      # Pipx
      - name: Pipx - Install
        run: python -m pip install --upgrade pipx

      # Homebrew
      - name: Homebrew - macOS upgrade
        if: runner.os == 'macOS'
        run: |
          brew upgrade
          brew update
      - name: Homebrew - Linux install
        if: runner.os == 'Linux'
        run: |
          /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
          echo "/home/linuxbrew/.linuxbrew/bin/" >> "$GITHUB_PATH"
      - name: Homebrew - Doctor and tap repair
        if: runner.os != 'Windows'
        run: |
          brew doctor || true
          brew tap --repair

      # Apt
      - name: APT - Linux upgrade
        if: runner.os == 'Linux'
        run: |
          sudo apt-get --quiet --yes update
          # lxml has no pre-built wheel for development Python (3.15+), so it
          # must compile from source, which needs libxml2 and libxslt headers.
          sudo apt-get --quiet --yes install libxml2-dev libxslt-dev

      # Aptitude
      - name: Aptitude - Linux install
        if: runner.os == 'Linux'
        # The second dpkg front-end these runners can carry, so it is what
        # exercises the `dpkg lock` xdist group against a real backend lock:
        # `apt` alone never proves the serialization works.
        run: sudo apt-get --quiet --yes install --no-install-recommends aptitude

      # PEAR
      - name: PEAR - Linux install
        if: runner.os == 'Linux'
        # Carries no recommends of its own, so the flag only guards against a
        # dependency growing some later. Homebrew's PHP ships no `pear`, which
        # is why there is no macOS counterpart.
        run: sudo apt-get --quiet --yes install --no-install-recommends php-pear

      # PackageKit
      - name: PackageKit - Linux install
        if: runner.os == 'Linux'
        run: sudo apt-get --quiet --yes install packagekit

      # Chocolatey
      - name: Chocolatey - Windows self-upgrade
        if: runner.os == 'Windows'
        run: choco upgrade chocolatey --no-progress --yes --retry-count=3
      - name: Chocolatey - Pin un-upgradeable packages
        if: runner.os == 'Windows'
        # XXX The "choco upgrade all" step below fails for some packages (strawberryperl MSI error, rtools, nginx
        # port conflict). Pin them to prevent upgrade failures.
        run: |
          choco pin add --name="strawberryperl"
          choco pin add --name="rtools"
          choco pin add --name="nginx"
      - name: Chocolatey - Windows full upgrade
        if: runner.os == 'Windows'
        # Best-effort: upgrade all packages preemptively as the test suite will attempt it but fail because of mpm's
        # enforced timeout. The Chocolatey CDN occasionally returns 503 for individual packages, causing a non-zero
        # exit code despite --ignore-package-exit-codes (which only covers installer exit codes, not download errors).
        # Force exit 0 so transient CDN failures don't block the test suite.
        run: |
          choco upgrade all --no-progress --yes --ignore-package-exit-codes --ignore-detected-reboot --retry-count=3
          exit 0

      # NPM
      - name: NPM - Linux install
        if: runner.os == 'Linux'
        run: |
          sudo apt-get --quiet --yes install npm
          sudo npm install --global npm@latest
      - name: NPM - macOS install
        if: runner.os == 'macOS'
        run: brew install npm
      - name: NPM - Windows upgrade
        if: runner.os == 'Windows'
        run: npm install --global npm # zizmor: ignore[adhoc-packages]

      # pnpm
      # Installed through npm, which the steps above set up on every OS (Node itself is
      # pre-installed on all GitHub-hosted runners), so one cross-platform step suffices.
      - name: pnpm - install
        run: npm install --global pnpm # zizmor: ignore[adhoc-packages]

      # Cargo
      - name: Cargo - macOS install
        if: runner.os == 'macOS'
        run: brew install rust
      - name: Cargo - Linux install
        if: runner.os == 'Linux'
        run: |
          sudo apt-get --quiet --yes install cargo
          cargo --version
      - name: Cargo - Windows install
        if: runner.os == 'Windows'
        run: choco install rust --no-progress --yes --retry-count=3

      # Yarn
      - name: Yarn - macOS install
        if: runner.os == 'macOS'
        run: brew install yarn

      # XXX Yarn is only installed on macOS. On Linux and Windows, Yarn's post-install binary path is not registered
      # in PATH within the same GitHub Actions shell session, causing FileNotFoundError during mpm upgrade tests.

      # Composer
      - name: Composer - macOS install
        if: runner.os == 'macOS'
        run: brew install composer
      - name: Composer - Windows upgrade
        if: runner.os == 'Windows'
        run: choco upgrade composer --no-progress --yes --retry-count=3

      # MAS
      - name: MAS - macOS install
        if: runner.os == 'macOS'
        run: brew install mas

      # Flatpak
      - name: Flatpak - Linux install
        if: runner.os == 'Linux'
        run: sudo apt-get --quiet --yes install flatpak

      # PowerShell Gallery
      - name: PowerShell Gallery - macOS install
        if: runner.os == 'macOS'
        # GitHub-hosted macOS images ship pwsh 7.x and PSResourceGet, but install both defensively in case the image
        # has not been refreshed yet on a given runner.
        run: |
          brew install --cask powershell || true
          pwsh -NoProfile -NonInteractive \
            -Command "Install-Module -Name Microsoft.PowerShell.PSResourceGet -Force -AcceptLicense -Scope CurrentUser"
      - name: PowerShell Gallery - Linux install
        if: runner.os == 'Linux'
        # See: https://learn.microsoft.com/en-us/powershell/scripting/install/install-ubuntu
        run: |
          sudo snap install powershell --classic
          pwsh -NoProfile -NonInteractive \
            -Command "Install-Module -Name Microsoft.PowerShell.PSResourceGet -Force -AcceptLicense -Scope CurrentUser"
      - name: PowerShell Gallery - Windows bootstrap
        if: runner.os == 'Windows'
        shell: pwsh
        # pwsh 7.4+ is pre-installed on windows-latest runners. Refresh PSResourceGet to the latest published version so
        # the bundled copy does not lag behind the version mpm exercises in tests.
        run: Install-Module -Name Microsoft.PowerShell.PSResourceGet -Force -AcceptLicense -Scope CurrentUser

      # Scoop
      - name: Scoop - Windows install
        if: runner.os == 'Windows'
        shell: pwsh
        # Retries absorb the transient connection resets get.scoop.sh serves
        # the runners every now and then.
        run: |
          iwr get.scoop.sh -outfile 'install.ps1' -MaximumRetryCount 3 -RetryIntervalSec 10
          .\install.ps1 -RunAsAdmin
          "$HOME\scoop\shims" | Out-File -Append -FilePath $env:GITHUB_PATH

      # SFSU
      - name: SFSU - Windows install
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          scoop bucket add extras
          scoop install sfsu

      # WinGet
      - name: WinGet - Windows install
        if: runner.os == 'Windows'
        # XXX Force shell configuration to PowerShell, even if it is the default for Windows runners, so the actionlint
        # job from the lint.yaml workflow will not try to validate the command syntax with shellcheck.
        shell: pwsh
        # Ignore exit code, as WinGet will fail if it is already up-to-date.
        run: >
          winget upgrade winget --accept-package-agreements --accept-source-agreements --disable-interactivity
          || & { "ignore failure"; $global:LASTEXITCODE = 0 }

      # DNF
      - name: DNF - Linux install
        if: runner.os == 'Linux'
        run: sudo apt-get --quiet --yes install dnf

      # Zypper
      - name: Zypper - Linux install
        if: runner.os == 'Linux'
        run: sudo apt-get --quiet --yes install zypper

      # Snap
      - name: Snap - Linux upgrade
        if: runner.os == 'Linux'
        run: sudo apt-get --quiet --yes install snapd

      # VSCode
      - name: VSCode - macOS install
        if: runner.os == 'macOS'
        run: brew install --cask visual-studio-code
      - name: VSCode - Linux install
        # XXX VS Code snap is only available for amd64, not arm64.
        # The snap store is intermittently flaky on Ubuntu 24.04 runners, so retry a few times before giving up.
        if: runner.os == 'Linux' && runner.arch == 'X64'
        run: |
          for attempt in 1 2 3; do
              if sudo snap install --classic code; then
                  exit 0
              fi
              echo "snap install code failed (attempt ${attempt}/3), retrying in 10s..."
              sleep 10
          done
          exit 1
      - name: VSCode - Windows install
        if: runner.os == 'Windows'
        run: choco install vscode --no-progress --yes --retry-count=3

      # vim-pack
      # https://formulae.brew.sh/formula/neovim
      # https://snapcraft.io/nvim
      # https://community.chocolatey.org/packages/neovim
      # vim.pack ships in Neovim's core since 0.12, so mpm requires >=0.12.0 and reports
      # the manager unavailable below that. Ubuntu still packages 0.11 in apt, hence the
      # snap: it tracks Neovim's own stable tag and builds for both amd64 and arm64.
      - name: Neovim - macOS install
        if: runner.os == 'macOS'
        run: brew install neovim
      - name: Neovim - Linux install
        if: runner.os == 'Linux'
        run: sudo snap install --classic nvim
      - name: Neovim - Windows install
        if: runner.os == 'Windows'
        run: choco install neovim --no-progress --yes --retry-count=3

      # Gem
      - name: Ruby & Gem - Windows upgrade
        if: runner.os == 'Windows'
        # The 'refreshenv' call forces the new Ruby version installed by 'choco' to be registered in the 'PATH'.
        # The Import-Module workaround is inspired by:
        # https://github.com/actions/runner-images/discussions/6065
        # https://docs.chocolatey.org/en-us/troubleshooting#why-does-choco-tab-not-work-for-me
        run: |
          choco upgrade ruby --no-progress --yes --retry-count=3
          Import-Module "$env:ChocolateyInstall\helpers\chocolateyProfile.psm1"
          refreshenv
          gem update --quiet --system
      - name: Ruby & Gem - macOS
        if: runner.os == 'macOS'
        run: sudo gem update --quiet --system
      - name: Ruby & Gem - Ubuntu
        # As of Ubuntu 21.04, RubyGems is exclusively managed by APT; "gem update --system" is unsupported.
        if: runner.os == 'Linux'
        run: sudo apt-get --quiet --yes install ruby-rubygems

      # SteamCMD
      - name: SteamCMD - macOS install
        if: runner.os == 'macOS'
        run: brew install --cask steamcmd
      - name: SteamCMD - Linux install
        # XXX Steam and SteamCMD packages are only available for x86, not arm64.
        if: runner.os == 'Linux' && runner.arch == 'X64'
        # Some notes on the Steam situation on Linux:
        # https://popey.com/blog/2023/08/i386-in-ubuntu-wont-die/
        run: |
          sudo add-apt-repository --yes multiverse
          sudo dpkg --add-architecture i386
          sudo apt-get --quiet --yes update
          # Accept Steam license.
          # Source: https://askubuntu.com/a/1017487
          echo steam steam/question select "I AGREE" | sudo debconf-set-selections
          echo steam steam/license note '' | sudo debconf-set-selections
          sudo debconf-show steam
          sudo apt-get --quiet --yes install libgcc-s1:i386 libstdc++6:i386 libatomic1:i386
          sudo apt-get --quiet --yes install steam
          sudo apt-get --quiet --yes install steamcmd
      - name: SteamCMD - Windows install
        if: runner.os == 'Windows'
        run: choco install steamcmd --no-progress --yes --retry-count=3

      # Conda
      # Miniconda ships pre-installed on GitHub-hosted runners at $CONDA but is kept off
      # PATH. Expose its binary so mpm detects conda, guarding on $CONDA so any image that
      # lacks it (some ARM runners) skips conda instead of failing this step. On Linux and
      # macOS $CONDA is root-owned, so hand the base environment to the unprivileged runner
      # user, otherwise the destructive install/remove test cannot mutate it.
      - name: Conda - Linux & macOS setup
        if: runner.os != 'Windows'
        run: |
          if [ -n "${CONDA:-}" ] && [ -d "$CONDA" ]; then
            sudo chown -R "$USER" "$CONDA"
            echo "$CONDA/bin" >> "$GITHUB_PATH"
          fi
      - name: Conda - Windows setup
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          if ($env:CONDA -and (Test-Path $env:CONDA)) {
            "$env:CONDA\Scripts" | Out-File -Append -FilePath $env:GITHUB_PATH
            "$env:CONDA\condabin" | Out-File -Append -FilePath $env:GITHUB_PATH
          }

      # Pixi
      # https://formulae.brew.sh/formula/pixi
      # https://github.com/ScoopInstaller/Main/blob/master/bucket/pixi.json
      # Not preinstalled on any runner image. Homebrew covers macOS and Linux (the
      # formula is bottled for both), Scoop covers Windows; both were bootstrapped
      # by earlier steps of this job.
      - name: Pixi - Linux & macOS install
        if: runner.os != 'Windows'
        run: brew install pixi
      - name: Pixi - Windows install
        if: runner.os == 'Windows'
        shell: pwsh
        run: scoop install main/pixi

      # The .NET SDK is preinstalled on every runner image this matrix uses, so
      # `dotnet tool` needs no setup step.

      - name: Install UV
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          # Pin what CI downloads: unpinned, setup-uv installs the newest uv
          # satisfying `required-version`. `sync-workflow-pins` bumps it once a
          # uv release clears the cooldown window.
          version: "0.12.5"
      - name: Install Python ${{ matrix.python-version }}
        # If UV cannot find the requested Python version, it exits with code 2, which let the job pass unnoticed on
        # Windows. So we force the shell to bash, even on Windows, and "set -e" to ensure any error in the install
        # process is caught and fails the job. It works because Windows runners too Git Bash available).
        shell: bash
        run: |
          set -e
          uv --no-progress venv --python "${PYTHON_VERSION}"
        env:
          PYTHON_VERSION: ${{ matrix.python-version }}
      - name: Install project
        run: uv --no-progress sync --frozen --all-extras --group test

      # mpm CLI runs.
      - name: Run local CLI
        run: uv run -- mpm --version
      - name: Run CLI from module
        run: |
          uv run        -m meta_package_manager --version
          uv run python -m meta_package_manager --version

      # Bar plugin runs.
      - name: uv run bar_plugin.py --search-mpm
        if: runner.os == 'macOS'
        run: uv --no-progress run --frozen -- ./meta_package_manager/bar_plugin.py --search-mpm
      - name: uv run bar_plugin.py
        if: runner.os == 'macOS'
        run: uv --no-progress run --frozen -- ./meta_package_manager/bar_plugin.py

      # Test suite runs.
      # This is the slice that holds the `[tool.coverage] report.fail_under`
      # ratchet: it walks the whole suite, so its number is the one worth
      # gating on. No `--cov-fail-under` is passed here, as the flag would
      # outrank the config and shadow the floor declared in pyproject.toml.
      #
      # `once`-marked tests are excluded from every matrix cell: the dedicated
      # `once-tests` job below runs them on a single runner instead. They
      # import no package code beyond `__version__`, so dropping them here
      # cannot move the coverage floor.
      - name: Parallel non-destructive tests
        run: >
          uv --no-progress run --frozen --
          pytest --exitfirst
          -m "not once"
          --cov
          --cov-report=term
          --numprocesses=auto --dist=loadgroup --maxschedchunk=1
          --run-non-destructive --skip-destructive
          --ignore=./tests/test_bar_plugin.py

      # Bar plugin calls way too many external commands to be run in parallel.
      # `--cov-fail-under=0` opts this slice out of the coverage ratchet: a
      # single test module covers ~33% of the package on its own by design.
      - name: Sequential, bar plugin tests - macOS
        if: runner.os == 'macOS'
        run: >
          uv --no-progress run --frozen --
          pytest --exitfirst
          --cov
          --cov-report=term
          --cov-fail-under=0
          --numprocesses=0 --run-non-destructive --run-destructive
          ./tests/test_bar_plugin.py

      # Run destructive tests last, as they alter the system packages. They are
      # scheduled by lock family: conftest.py stamps every destructive test with an
      # xdist group derived from dispatch.py's SHARED_LOCK_FAMILIES, so managers
      # contending for one backend lock stay serial on a single worker while
      # independent families run in parallel, the same policy mpm's own grouped
      # fan-out applies at runtime.
      # This run omits --exitfirst on purpose: walking every manager surfaces all
      # newly CI-unsupported managers in a single run instead of one per push cycle.
      # --maxfail=10 still caps a wholesale breakage. The runs above keep --exitfirst.
      # `--cov-fail-under=0` opts this slice out of the coverage ratchet: the
      # destructive subset covers ~48% of the package on its own by design.
      - name: Parallel destructive tests
        run: >
          uv --no-progress run --frozen --
          pytest --maxfail=10
          --cov
          --cov-report=term
          --cov-fail-under=0
          --numprocesses=auto --dist=loadgroup --maxschedchunk=1
          --skip-non-destructive --run-destructive
          -m "not destructive_all_managers"
          --ignore=./tests/test_bar_plugin.py
        env:
          # The runners ship gh unauthenticated, and gh refuses extension installs
          # without credentials. Feed it the workflow token so the gh-ext destructive
          # install/remove round-trip runs for real (see conftest.py's PACKAGE_IDS).
          GH_TOKEN: ${{ github.token }}

      # The cross-manager destructive tests drive every available manager in a
      # single invocation, so no lock-family grouping can isolate them from the
      # step above: they run on their own, sequentially, once the grouped tests
      # are done.
      # The full sync scope is re-declared here because the grouped step may
      # have upgraded the very interpreter the venv was built from (Homebrew's
      # python sits among macOS upgrade targets): uv then rebuilds the venv
      # from the replacement, and without the scope that rebuild carries only
      # the base dependencies, pytest not among them.
      # No coverage on this step: two tests cannot move a slice that already
      # opts out of the ratchet.
      # A single xdist worker rather than --numprocesses=0, and not for
      # parallelism: both tests still run sequentially on the one worker,
      # while the worker's shutdown cannot touch the controller's exit code
      # once its reports are in.
      # The invocation retries once because driving every Windows manager in
      # one process occasionally lets a manager's own subprocess teardown
      # broadcast a console control event that lands on pytest itself (a
      # KeyboardInterrupt mid-run under xdist, a clean all-passed summary
      # followed by a bare exit 1 without it). Both tests converge on re-run,
      # and a genuine failure fails both attempts.
      - name: Sequential cross-manager destructive tests
        shell: bash
        run: |
          cross_manager_tests() {
            uv --no-progress run --frozen --all-extras --group test -- \
              pytest --maxfail=10 \
              --numprocesses=1 --dist=loadgroup --skip-non-destructive --run-destructive \
              -m destructive_all_managers \
              --ignore=./tests/test_bar_plugin.py
          }
          cross_manager_tests || cross_manager_tests
        env:
          # Same rationale as the step above: gh-ext's upgrade needs credentials.
          GH_TOKEN: ${{ github.token }}

  once-tests:
    name: 1️⃣ Run-once tests
    # `needs: metadata` is not for its outputs but for its skip gate: the
    # version-bump branches it filters out must skip this job too.
    needs:
      - metadata
    runs-on: ubuntu-26.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-tags: true
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          # Pin what CI downloads: unpinned, setup-uv installs the newest uv
          # satisfying `required-version`. `sync-workflow-pins` bumps it once a
          # uv release clears the cooldown window.
          version: "0.12.5"
      - name: Install Python
        run: uv --no-progress venv --python 3.14
      - name: Install project
        run: uv --no-progress sync --frozen --all-extras --group test
      - name: Run-once unittests
        # `once`-marked tests assert on static project metadata and on the
        # GNOME Shell extension sources. They behave identically on every OS
        # and Python, so the matrix `tests` job excludes them and this single
        # runner is their only executor. No `--cov` here: they import no
        # package code beyond `__version__`, so measuring them is pointless
        # and would only invite a spurious floor check.
        run: uv --no-progress run --frozen -- pytest -m once
