# {octicon}`diff-added` Add a new package manager

% The skill body is the single source: this page renders it for humans, Claude reads it at /add-manager.
% :start-line: skips the frontmatter, the skill's own H1 (replaced by the one above) and the $ARGUMENTS
% line, so it must be bumped whenever any of those grow or shrink. Pinned by
% test_add_manager_skill_include_offset in tests/test_docs.py.

```{include} ../.claude/skills/add-manager/SKILL.md
:start-line: 12
```
