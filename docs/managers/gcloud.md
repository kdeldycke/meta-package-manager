# {octicon}`package` Google Cloud CLI

```{python:render}
from meta_package_manager._docs import manager_intro

print(manager_intro("gcloud"))
```

## What `mpm` adds to `gcloud`

```{python:render}
from meta_package_manager._docs import manager_augments

print(manager_augments("gcloud"))
```

## Your `gcloud` commands, in `mpm`

```{python:render}
from meta_package_manager._docs import manager_rosetta

print(manager_rosetta("gcloud"))
```

## Operations

```{python:render}
from meta_package_manager._docs import manager_operations

print(manager_operations("gcloud"))
```

## Selecting and configuring `gcloud`

```{python:render}
from meta_package_manager._docs import manager_selection

print(manager_selection("gcloud"))
```

## Recipes

```{python:render}
from meta_package_manager._docs import manager_recipes

print(manager_recipes("gcloud"))
```

## Privilege escalation

```{python:render}
from meta_package_manager._docs import manager_sudo

print(manager_sudo("gcloud"))
```

## Cooldown

```{python:render}
from meta_package_manager._docs import manager_cooldown

print(manager_cooldown("gcloud"))
```

## Version probe

```{python:render}
from meta_package_manager._docs import manager_version_probe

print(manager_version_probe("gcloud"))
```

## Reference traces

```{python:render}
from meta_package_manager._docs import manager_traces

print(manager_traces("gcloud"))
```

## Changelog

```{python:render}
from meta_package_manager._docs import manager_changelog

print(manager_changelog("gcloud"))
```
