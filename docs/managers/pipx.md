# {octicon}`package` Python pipx

```{python:render}
from docs_update import manager_intro

print(manager_intro("pipx"))
```

## What `mpm` adds to `pipx`

```{python:render}
from docs_update import manager_augments

print(manager_augments("pipx"))
```

## Your `pipx` commands, in `mpm`

```{python:render}
from docs_update import manager_rosetta

print(manager_rosetta("pipx"))
```

## Platforms

```{python:render}
from docs_update import manager_platforms

print(manager_platforms("pipx"))
```

## Operations

```{python:render}
from docs_update import manager_operations

print(manager_operations("pipx"))
```

## Ecosystem

```{python:render}
from docs_update import manager_ecosystem

print(manager_ecosystem("pipx"))
```

## Selecting and configuring `pipx`

```{python:render}
from docs_update import manager_selection

print(manager_selection("pipx"))
```

## Recipes

```{python:render}
from docs_update import manager_recipes

print(manager_recipes("pipx"))
```

## How `mpm` drives `pipx`

```{python:render}
from docs_update import manager_cli

print(manager_cli("pipx"))
```

## Privilege escalation

```{python:render}
from docs_update import manager_sudo

print(manager_sudo("pipx"))
```

## Cooldown

```{python:render}
from docs_update import manager_cooldown

print(manager_cooldown("pipx"))
```

## Reference traces

```{python:render}
from docs_update import manager_traces

print(manager_traces("pipx"))
```
