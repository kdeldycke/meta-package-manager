# Maintainer: Kevin Deldycke <kevin@deldycke.com>
pkgname=py3-extra-platforms
pkgver=13.8.0
pkgrel=0
pkgdesc="Detect platforms, OSes, distributions and group them by family"
url="https://github.com/kdeldycke/extra-platforms"
arch="noarch"
license="Apache-2.0"
depends="python3"
makedepends="py3-gpep517 py3-installer py3-uv-build"
checkdepends="py3-pytest"
subpackages="$pkgname-pyc"
source="https://files.pythonhosted.org/packages/source/e/extra-platforms/extra_platforms-$pkgver.tar.gz"
builddir="$srcdir/extra_platforms-$pkgver"

build() {
	gpep517 build-wheel \
		--wheel-dir .dist \
		--output-fd 3 3>&1 >&2
}

check() {
	# Run the sdist's test suite against the installed wheel, minus the network
	# tests (the build sandbox has no outbound network) and test_import_time,
	# which budgets a cold import in wall-clock milliseconds: it measures the
	# builder rather than the package, and the emulated arches exceed it.
	python3 -m venv --clear --without-pip --system-site-packages .testenv
	.testenv/bin/python3 -m installer .dist/*.whl
	.testenv/bin/python3 -m pytest -m "not network" \
		--deselect tests/test_root.py::test_import_time
}

package() {
	python3 -m installer -d "$pkgdir" \
		.dist/*.whl
}

sha512sums="
7532c1f99b11c38bf1e28c200fab7fdcb7661b994fa5635f0eac3db2aff5f838a0544a468632023f2ec85a6fb9a3311c7805aafc9d47e726561031c648f871cf  extra_platforms-13.8.0.tar.gz
"
