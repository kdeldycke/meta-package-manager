# This test plan checks some high-level behavior of the mpm CLI.

# Runs the same binary tests as in the brew official Formula:
# https://github.com/Homebrew/homebrew-core/blob/f0a4d69/Formula/meta-package-manager.rb#L401-L413

# Check mpm is reporting the correct version of itself. `--no-color` pins the plain
# rendering: it is the form machine readers parse (the bar plugin probes
# `mpm --no-color --version`), and the only one whose layout is exactly specified.
# With colors on, the same metadata is laid out beside the ANSI brand mark instead.
[[cases]]
cli_parameters = "--no-color --version"
exit_code = 0
stdout_regex_fullmatch = "mpm, version [0-9]+\\.[0-9]+\\.[0-9]+(\\.[a-z]+[0-9]+(\\+[a-f0-9]+)?)?\\nPython [^\\n]+\\n"

# Check ANSI reaches stdout, and that the brand mark renders there.
# click_extra 7.19+ renders the program name with both bright (97m) and bold (1m)
# codes, so the assertion below starts after them rather than pinning either.
# `--color` forces ANSI: the test harness captures stdout (no TTY), so without it
# the binary emits a plain, uncolored string and these assertions fail on every
# platform. XXX Skipped on Windows even so: colorama rewrites ANSI into Win32
# console calls, so the forced escape codes never reach stdout there.
# Deliberately no full-match: the mark is 10 lines of half-blocks, and it drops back
# to the plain rendering on a terminal too narrow to seat it. Asserting its lightest
# brand tone is enough, no other mpm output emitting that color.
[[cases]]
cli_parameters = "--color --version"
skip_platforms = "windows"
exit_code = 0
stdout_contains = [
    "mpm\u001b[0m, version \u001b[32m",
    "\u001b[38;5;189m",
]

# Check mpm is reporting its usage in help screen.
[[cases]]
cli_parameters = "--help"
exit_code = 0
strip_ansi = true
stdout_contains = [
    "Usage: mpm [OPTIONS] COMMAND [ARGS]...",
    "-h, --help",
]

# Check mpm is detecting brew and report it as a manager in a table row.
# XXX Windows has issues with ANSI colors, so we skip this test on Windows:
# Traceback (most recent call last):
#   File "<frozen runpy>", line 198, in _run_module_as_main
#   File "<frozen runpy>", line 88, in _run_code
#   File "C:\…\gha-utils.exe\__main__.py", line 10, in <module>
#   File "C:\…\gha_utils\__main__.py", line 45, in main
#     gha_utils(prog_name=gha_utils.name)
#   File "C:\…\click\core.py", line 1442, in __call__
#     return self.main(*args, **kwargs)
#            ^^^^^^^^^^^^^^^^^^^^^^^^^^
#   File "C:\…\click_extra\commands.py", line 338, in main
#     return super().main(*args, **kwargs)
#            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
#   File "C:\…\click\core.py", line 1363, in main
#     rv = self.invoke(ctx)
#          ^^^^^^^^^^^^^^^^
#   File "C:\…\click_extra\commands.py", line 368, in invoke
#     return super().invoke(ctx)
#            ^^^^^^^^^^^^^^^^^^^
#   File "C:\…\click\core.py", line 1830, in invoke
#     return _process_result(sub_ctx.command.invoke(sub_ctx))
#                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
#   File "C:\…\click_extra\commands.py", line 368, in invoke
#     return super().invoke(ctx)
#            ^^^^^^^^^^^^^^^^^^^
#   File "C:\…\click\core.py", line 1226, in invoke
#     return ctx.invoke(self.callback, **ctx.params)
#            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
#   File "C:\…\click\core.py", line 794, in invoke
#     return callback(*args, **kwargs)
#            ^^^^^^^^^^^^^^^^^^^^^^^^^
#   File "C:\…\gha_utils\cli.py", line 409, in test_plan
#     echo(test_case.execution_trace or "No execution trace available.")
#   File "C:\…\click\utils.py", line 321, in echo
#     file.write(out)  # type: ignore
#     ^^^^^^^^^^^^^^^
#   File "C:\…\click\_compat.py", line 543, in _safe_write
#     return _write(s)
#            ^^^^^^^^^
#   File "C:\…\colorama\ansitowin32.py", line 47, in write
#     self.__convertor.write(text)
#   File "C:\…\colorama\ansitowin32.py", line 179, in write
#     self.wrapped.write(text)
#   File "C:\…\Python\3.11.9\x64\Lib\encodings\cp1252.py", line 19, in encode
#     return codecs.charmap_encode(input,self.errors,encoding_table)[0]
#            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
# UnicodeEncodeError: 'charmap' codec can't encode character '►' in position 0: character maps to <undefined>
[[cases]]
cli_parameters = "--color --table-format csv --all-managers managers"
skip_platforms = "windows"
exit_code = 0
strip_ansi = true
# cargo is fully detected on macOS runners, but the Linux binary is built and
# tested inside a minimal manylinux container that has never shipped a Rust
# toolchain: accept dnf (confirmed present with version there) as the Linux
# stand-in example of a fully-detected manager.
stdout_regex_matches = "(cargo,Rust cargo|dnf,Fedora DNF),✓,✓ \\S+,✓,✓ \\S+\\n"

# Same as above, but without colors.
# XXX Windows has issues with ANSI colors, so we skip this test on Windows:
# Traceback (most recent call last):
#   File "<frozen runpy>", line 198, in _run_module_as_main
#   File "<frozen runpy>", line 88, in _run_code
#   File "C:\…\gha-utils.exe\__main__.py", line 10, in <module>
#   File "C:\…\gha_utils\__main__.py", line 45, in main
#     gha_utils(prog_name=gha_utils.name)
#   File "C:\…\click\core.py", line 1442, in __call__
#     return self.main(*args, **kwargs)
#            ^^^^^^^^^^^^^^^^^^^^^^^^^^
#   File "C:\…\click_extra\commands.py", line 338, in main
#     return super().main(*args, **kwargs)
#            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
#   File "C:\…\click\core.py", line 1363, in main
#     rv = self.invoke(ctx)
#          ^^^^^^^^^^^^^^^^
#   File "C:\…\click_extra\commands.py", line 368, in invoke
#     return super().invoke(ctx)
#            ^^^^^^^^^^^^^^^^^^^
#   File "C:\…\click\core.py", line 1830, in invoke
#     return _process_result(sub_ctx.command.invoke(sub_ctx))
#                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
#   File "C:\…\click_extra\commands.py", line 368, in invoke
#     return super().invoke(ctx)
#            ^^^^^^^^^^^^^^^^^^^
#   File "C:\…\click\core.py", line 1226, in invoke
#     return ctx.invoke(self.callback, **ctx.params)
#            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
#   File "C:\…\click\core.py", line 794, in invoke
#     return callback(*args, **kwargs)
#            ^^^^^^^^^^^^^^^^^^^^^^^^^
#   File "C:\…\gha_utils\cli.py", line 409, in test_plan
#     echo(test_case.execution_trace or "No execution trace available.")
#   File "C:\…\click\utils.py", line 321, in echo
#     file.write(out)  # type: ignore
#     ^^^^^^^^^^^^^^^
#   File "C:\…\click\_compat.py", line 543, in _safe_write
#     return _write(s)
#            ^^^^^^^^^
#   File "C:\…\colorama\ansitowin32.py", line 47, in write
#     self.__convertor.write(text)
#   File "C:\…\colorama\ansitowin32.py", line 179, in write
#     self.wrapped.write(text)
#   File "C:\…\Python\3.11.9\x64\Lib\encodings\cp1252.py", line 19, in encode
#     return codecs.charmap_encode(input,self.errors,encoding_table)[0]
#            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
# UnicodeEncodeError: 'charmap' codec can't encode character '►' in position 0: character maps to <undefined>
[[cases]]
cli_parameters = "--no-color --table-format csv --all-managers managers"
skip_platforms = "windows"
exit_code = 0
# XXX --no-color does not apply to CSV export: https://github.com/kdeldycke/meta-package-manager/issues/1004
# So we force the ANSI stripping.
strip_ansi = true
# See the color variant above for why dnf is accepted alongside cargo.
stdout_regex_matches = [
    "(cargo,Rust cargo|dnf,Fedora DNF),✓,✓ \\S+,✓,✓ \\S+\\n",
]

# Check some common packages are reported as installed in CSV format (Linux).
# extra-platforms 13.6.0 recognizes the manylinux_2_28 test container as
# AlmaLinux/Fedora-family, so tzdata is now correctly attributed to dnf/yum
# there instead of falling through to apt/uv.
[[cases]]
cli_parameters = "--table-format csv installed"
only_platforms = "linux"
exit_code = 0
stdout_regex_matches = [
    "tzdata,,(apt|dnf|uv|yum),\\S+\\n",
]

# Check some common packages are reported as installed in CSV format (macOS).
[[cases]]
cli_parameters = "--table-format csv installed"
only_platforms = "macos"
exit_code = 0
stdout_regex_matches = [
    "git,,brew,\\S+\\n",
]

# Check some common packages are reported as installed in CSV format (Windows).
# XXX We cannot rely on `chocolatey` self-listing via `choco list` on the
# windows-2025 runner: `choco` is detected as available (version check passes)
# but `choco list --limit-output` returns an empty package list, causing
# `choco: 0` in the pool summary with no error logged. Until that environment
# issue is diagnosed, match `click` instead: it is an mpm dependency and is
# guaranteed to be present in the workflow's `uv` environment.
[[cases]]
cli_parameters = "--table-format csv installed"
only_platforms = "windows"
exit_code = 0
stdout_regex_matches = [
    "click,,uv,\\S+\\n",
]

[[cases]]
cli_parameters = "managers"
skip_platforms = "windows"
exit_code = 0

[[cases]]
cli_parameters = "--all-managers managers"
skip_platforms = "windows"
exit_code = 0

[[cases]]
cli_parameters = "installed"
skip_platforms = "windows"
exit_code = 0

[[cases]]
cli_parameters = "outdated"
skip_platforms = "windows"
exit_code = 0

# XXX `which` prints a sorted table; on Windows the box-drawing and ✓ glyphs
# raise a cp1252 UnicodeEncodeError, so skip it there like the other table cases.
[[cases]]
cli_parameters = "which mpm"
skip_platforms = "windows"
exit_code = 0

[[cases]]
cli_parameters = "backup"
exit_code = 0

[[cases]]
cli_parameters = "sbom --spdx"
exit_code = 0

[[cases]]
cli_parameters = "sbom --cyclonedx"
exit_code = 0

# Config-format coverage. Each fixture is a minimal valid `[mpm]` config; the
# matching format reader is gated behind `try/except import <pkg>` in
# click-extra, so a failing case here means the [pkg]-named extra is missing
# from the binary (either not synced via [tool.repomatic] nuitka.extras or
# not bundled via [tool.nuitka] include-package).
[[cases]]
cli_parameters = "--validate-config tests/cli-test-plan-config.toml"
exit_code = 0
stderr_contains = "is valid."

[[cases]]
cli_parameters = "--validate-config tests/cli-test-plan-config.yaml"
exit_code = 0
stderr_contains = "is valid."

[[cases]]
cli_parameters = "--validate-config tests/cli-test-plan-config.json"
exit_code = 0
stderr_contains = "is valid."

[[cases]]
cli_parameters = "--validate-config tests/cli-test-plan-config.json5"
exit_code = 0
stderr_contains = "is valid."

[[cases]]
cli_parameters = "--validate-config tests/cli-test-plan-config.jsonc"
exit_code = 0
stderr_contains = "is valid."

[[cases]]
cli_parameters = "--validate-config tests/cli-test-plan-config.hjson"
exit_code = 0
stderr_contains = "is valid."

[[cases]]
cli_parameters = "--validate-config tests/cli-test-plan-config.xml"
exit_code = 0
stderr_contains = "is valid."

# Table-format breadth. Each format is a separate tabulate renderer (toml,
# yaml, json, xml). A regression here flags a missing tabulate/format
# package data file in the bundled binary.
#
# The manager named is incidental: every case below only needs one that the
# host always detects, so each accepts dnf alongside cargo for the reason the
# CSV cases above give.
[[cases]]
cli_parameters = "--table-format json managers"
skip_platforms = "windows"
exit_code = 0
stdout_regex_matches = "\"id\": \"(cargo|dnf)\""

[[cases]]
cli_parameters = "--table-format yaml managers"
skip_platforms = "windows"
exit_code = 0
stdout_regex_matches = "id: (cargo|dnf)"

[[cases]]
cli_parameters = "--table-format toml managers"
skip_platforms = "windows"
exit_code = 0
stdout_regex_matches = "id = \"(cargo|dnf)\""

[[cases]]
cli_parameters = "--table-format xml managers"
skip_platforms = "windows"
exit_code = 0
stdout_regex_matches = "<(cargo|dnf)>"

# Globals introduced this cycle. `--man` exercises the roff renderer click-extra
# 7.19 ships and its embedded data files; `--summary` replaces the old
# `--stats`; `--accessible` swaps to the plain renderer; `--cooldown` parses
# both halves of its union, a duration string and an enforcement keyword. The
# introspection flags `--params` and
# `--man` print and exit 0 without contacting any package manager.
[[cases]]
cli_parameters = "--man"
exit_code = 0
stdout_contains = ".TH \"MPM\""

[[cases]]
cli_parameters = "--params"
skip_platforms = "windows"
exit_code = 0
strip_ansi = true
stdout_contains = "mpm.accessible"

[[cases]]
cli_parameters = "--accessible managers"
skip_platforms = "windows"
exit_code = 0
stdout_regex_matches = "(cargo|dnf)"

[[cases]]
cli_parameters = "--summary managers"
skip_platforms = "windows"
exit_code = 0
stdout_regex_matches = "(cargo|dnf)"

[[cases]]
cli_parameters = "--cooldown 30days managers"
skip_platforms = "windows"
exit_code = 0
stdout_regex_matches = "(cargo|dnf)"

[[cases]]
cli_parameters = "--cooldown best-effort managers"
skip_platforms = "windows"
exit_code = 0
stdout_regex_matches = "(cargo|dnf)"

# Subcommands not exercised by the other tests. `config-template` prints a
# per-manager TOML overrides skeleton, `help` dispatches click-extra's
# command-help renderer, `search` exercises the search highlighting path
# (click-extra theme).
[[cases]]
cli_parameters = "config-template"
exit_code = 0
stdout_contains = "[mpm.managers.apk]"

[[cases]]
cli_parameters = "help managers"
exit_code = 0
strip_ansi = true
stdout_contains = "Usage: mpm managers"

[[cases]]
cli_parameters = "search click"
skip_platforms = "windows"
exit_code = 0

# Negative cases: click-extra must surface usage errors with exit code 2 and
# a "No such" message, not a Python traceback (which the onefile binary has
# been known to print when an unexpected exception escapes click).
[[cases]]
cli_parameters = "bogus-subcommand"
exit_code = 2
stderr_contains = "No such command 'bogus-subcommand'"

[[cases]]
cli_parameters = "--bogus-flag"
exit_code = 2
stderr_contains = "No such option '--bogus-flag'"

# Every command and subcommand must render its --help screen without crashing,
# including in the compiled binaries where a missing data file or lazy import
# only surfaces at runtime. One case per command path, in the CLI's own sorted
# order; the root --help is covered by its dedicated case above.
# tests/test_help.py keeps this roster in sync with the live command tree.

[[cases]]
cli_parameters = "cleanup --help"
exit_code = 0
strip_ansi = true
stdout_contains = "Usage: mpm cleanup"

[[cases]]
cli_parameters = "config-template --help"
exit_code = 0
strip_ansi = true
stdout_contains = "Usage: mpm config-template"

[[cases]]
cli_parameters = "doctor --help"
exit_code = 0
strip_ansi = true
stdout_contains = "Usage: mpm doctor"

[[cases]]
cli_parameters = "dump --help"
exit_code = 0
strip_ansi = true
stdout_contains = "Usage: mpm dump"

[[cases]]
cli_parameters = "help --help"
exit_code = 0
strip_ansi = true
stdout_contains = "Usage: mpm help"

[[cases]]
cli_parameters = "install --help"
exit_code = 0
strip_ansi = true
stdout_contains = "Usage: mpm install"

[[cases]]
cli_parameters = "installed --help"
exit_code = 0
strip_ansi = true
stdout_contains = "Usage: mpm installed"

[[cases]]
cli_parameters = "managers --help"
exit_code = 0
strip_ansi = true
stdout_contains = "Usage: mpm managers"

[[cases]]
cli_parameters = "orphans --help"
exit_code = 0
strip_ansi = true
stdout_contains = "Usage: mpm orphans"

[[cases]]
cli_parameters = "outdated --help"
exit_code = 0
strip_ansi = true
stdout_contains = "Usage: mpm outdated"

[[cases]]
cli_parameters = "remove --help"
exit_code = 0
strip_ansi = true
stdout_contains = "Usage: mpm remove"

[[cases]]
cli_parameters = "restore --help"
exit_code = 0
strip_ansi = true
stdout_contains = "Usage: mpm restore"

[[cases]]
cli_parameters = "sbom --help"
exit_code = 0
strip_ansi = true
stdout_contains = "Usage: mpm sbom"

[[cases]]
cli_parameters = "search --help"
exit_code = 0
strip_ansi = true
stdout_contains = "Usage: mpm search"

[[cases]]
cli_parameters = "sync --help"
exit_code = 0
strip_ansi = true
stdout_contains = "Usage: mpm sync"

[[cases]]
cli_parameters = "upgrade --help"
exit_code = 0
strip_ansi = true
stdout_contains = "Usage: mpm upgrade"

[[cases]]
cli_parameters = "which --help"
exit_code = 0
strip_ansi = true
stdout_contains = "Usage: mpm which"
